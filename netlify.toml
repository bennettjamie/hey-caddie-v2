[build]
  command = "npm run build"
  # Netlify Next.js plugin handles publish directory automatically

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20.9.0"
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_ENV = "production"
